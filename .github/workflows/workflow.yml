name: Testing Workflow

on:
  push:
    branches: [feature/create-workflow]

jobs:
  my_first_job:
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.1

      - name: Workflow API
        uses: ./.github/actions/workflow-api

      - name: Workflow UI
        uses: ./.github/actions/workflow-ui